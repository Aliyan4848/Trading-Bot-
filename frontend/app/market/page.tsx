import { SectionPlaceholder } from "@/components/SectionPlaceholder";

export const metadata = { title: "Market Monitor — AI Trading Bot" };

export default function MarketPage() {
  return (
    <SectionPlaceholder
      title="Market Monitor"
      note="Live price, bid/ask, spread, chart and bar timeframes — built in Phase 7."
    />
  );
}
