import { SectionPlaceholder } from "@/components/SectionPlaceholder";

export const metadata = { title: "Trading Signals — AI Trading Bot" };

export default function SignalsPage() {
  return (
    <SectionPlaceholder
      title="Trading Signals"
      note="Recent BUY / SELL / NO TRADE signals with reasoning and outcomes — built in Phase 7."
    />
  );
}
