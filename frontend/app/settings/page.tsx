import { SectionPlaceholder } from "@/components/SectionPlaceholder";

export const metadata = { title: "Settings — AI Trading Bot" };

export default function SettingsPage() {
  return (
    <SectionPlaceholder
      title="Settings"
      note="Demo account connection, instruments, strategy parameters, trading mode — built in Phase 7. Secrets are never displayed."
    />
  );
}
